server:
  port: 8083

spring:
  application:
    name: questionnaire-service
  datasource:
    url: ${QUESTIONNAIRE_DB_URL:jdbc:postgresql://localhost:5432/questionnaire_service}
    username: ${QUESTIONNAIRE_DB_USER:questionnaire_service}
    password: ${QUESTIONNAIRE_DB_PASSWORD:questionnaire_service}
  jpa:
    open-in-view: false
    properties:
      hibernate.jdbc.time_zone: UTC
  flyway:
    enabled: true
    baseline-on-migrate: true
  rabbitmq:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:guest}
    password: ${RABBITMQ_PASSWORD:guest}
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8180/realms/tmpmgmt}

tmpmgmt:
  outbox:
    enabled: true
    publisher-enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus

logging:
  pattern:
    level: "%5p [%X{correlationId:-}]"
